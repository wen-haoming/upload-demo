{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "init":"lerna clean --yes && lerna bootstrap",
	"start":"lerna run start --parallel"
  }
}
